version: '3.7'
services:
  db:
    environment:
      POSTGRES_PASSWORD: change-me
    # This port section is optional if you want to be able to access the DB from your host machine
    ports:
      - 5432:5432
